Ранее, в рамках доработки механизма экспорта данных визуализаций и таблиц в формате "Excel" (XLSX) на нашем аналитическом портале, были созданы и реализованны следующие постановки:
1) ./XLSX_экспорт_Шаг_1.txt
2) ./XLSX_экспорт_Шаг_2.txt
3) ./Значения_фильтров_по_другому_полю_1.txt
4) ./Значения_фильтров_по_другому_полю_2.txt
5) ./Значения_фильтров_по_другому_полю_3.txt
6) ./XLSX_экспорт_Отображение_фильтров_по_другому_полю.txt
7) ./XLSX_экспорт_Отображение_значений_переменных.txt

Изучи данные постановки для понимания контекста.

Требуется проработать порядок отображения фильтров при экспорте в XLSX. Порядок должен соответствовать порядку фильтров на полке фильтров визуализации.

Критерии приёмки:

1) Порядок фильтров в экспортируемом XLSX совпадает с порядком на полке фильтров визуализации.
2) Поведение единообразно для разных типов фильтров.


Как системный аналитик с большим стажем, опираясь на изученные материалы и контекст, сформулируй логику работы и необходимые доработки нашего аналитического портала для реализации необходимой описанной функциональности. Сформулируй и подготовь понятную постановку, новая постановка должна полностью согласовываться с уже созданными постановками, которые ты изучил в контексте, но не нужно упоминать в этой постановке факты и обсуждения не касающиеся именно текущей доработки.

Постановка должна декларативно давать разработчикам информацию о необходимых доработках и не фокусироваться на изменениях внесенных в постановку, обсуждениях, согласованиях, других постановках и прочих некритичных деталях в рамках реализации данной функциональности.

Постановка должна быть верной и согласованной. Сохраняй оптимальную разметку и структуру документов, которая уже используется в первых постановках, для публикации в Jira.

Запиши финальную постановку по данной теме в файл ./XLSX_экспорт_порядок_отображения_фильтров.txt




