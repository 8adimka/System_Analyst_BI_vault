h4. 6. Передача типов данных с фронтенда на бэкенд для форматирования фильтров при экспорте.

Для корректного форматирования значений фильтров по правилам из пп. 1–4 бэкенд должен получать от фронтенда явный тип данных для каждого фильтра. 
Тип передаётся в новом обязательном поле *{{filterType}}* внутри каждого объекта фильтра в блоке {{export.parameters.filter}}.

h5. 6.1. Расширение структуры объекта фильтра.

Текущий формат объекта фильтра:
{code:json}
{
  "name": "ID менеджера",
  "values": "[1; 2; 3; 4; 5; 6; 7; 8]"
}
{code}

*Требуемый формат* (добавляется поле {{filterType}} и, опционально, {{dateFormat}}):
{code:json}
{
  "name": "ID менеджера",
  "values": "[1; 2; 3; 4; 5; 6; 7; 8]",
  "filterType": "number"
}
{code}

h5. 6.2. Допустимые значения поля {{filterType}}.

|| Значение {{filterType}} || Применяемое правило форматирования ||
| {{string}} | Строковые значения оборачиваются в одинарные кавычки (см. п. 1). |
| {{number}} | Числовые значения выводятся без кавычек, с разделителями разрядов (см. пп. 1, 2). |
| {{year}} | Числовые значения выводятся без кавычек, *без* разделителей разрядов (см. п. 2, исключение для года). |
| {{date}} | Значения форматируются по формату из настроек визуализации или дефолту {{ДД.ММ.ГГГГ}}, без кавычек (см. п. 3). |
| {{datetime}} | Значения форматируются как дата со временем по формату визуализации или дефолту, без кавычек (см. п. 3). |
| {{calculated}} | Базово фронтенд *должен* разрешить тип до одного из базовых: {{string}}, {{number}}, {{date}}, {{datetime}}. |

h5. 6.3. Особые требования к вычисляемым полям ({{fieldType: "calculated"}}).

Текущее поведение: в блоке {{export.parameters.filter}} вычисляемые поля передаются без явного типа.
Бэкенд не может самостоятельно определить итоговый тип вычисляемого выражения.

*Требование:* Фронтенд *должен*, если это возможно, разрешать итоговый тип вычисляемого поля и передавать его в поле {{filterType}} — 
*не* значение {{"calculated"}}, а один из базовых типов: {{"string"}}, {{"number"}}, {{"date"}}, {{"datetime"}}, {{"year"}}.

*Источник для определения типа:* фронтенд уже имеет метаинформацию о полях из Набора данных и тип результирующего выражения в спецификации визуализации. 
Логику определения типа реализовать аналогично тому, как уже реализовано для колонок таблицы в {{export.fieldType}}.

*Пример передачи фильтров по вычисляемым полям:*
{code:json}
{
  "filter": [
    {
      "name": "ВП_число_сумма_с_коэф",
      "values": "[1500000; 2400000]",
      "filterType": "number"
    },
    {
      "name": "ВП_строка_товар_регион",
      "values": "[Диван]",
      "filterType": "string"
    },
    {
      "name": "ВП_дата_заказа",
      "values": "[2024-01-01; 2024-12-31]",
      "filterType": "date"
    }
  ]
}
{code}

h5. 6.4. Передача формата даты.

Если для визуализации задан {{dateFormat}} в «Общих настройках» — фронтенд передаёт его на бэкенд в поле {{dateFormat}} рядом с {{filterType}}:

{code:json}
{
  "name": "Дата заказа",
  "values": "[2024-01-01; 2024-12-31]",
  "filterType": "date",
  "dateFormat": "DD.MM.YY"
}
{code}

Если поле {{dateFormat}} отсутствует или передано как {{null}} / пустая строка — бэкенд применяет дефолтный формат {{ДД.ММ.ГГГГ}} (соответствует 
{{DD.MM.YYYY}} в нотации moment.js).

*Приоритет на стороне фронтенда* при определении формата даты для передачи на бэкенд (аналогично логике в {{cellFormatters.js}}):
# {{column.dateFormat}} — формат на уровне конкретной колонки/полки;
# {{spec.dateFormat}} (он же {{config.dateFormat}}, попадает в {{spec}} через {{ReactabularSpecGenerator}}) — общий формат визуализации;
# Если ни один не задан или значение равно {{'\\-'}} — поле {{dateFormat}} не передаётся (бэкенд применяет дефолт).

h5. 6.5. Fallback-поведение бэкенда.

Для обеспечения обратной совместимости:
* Если поле {{filterType}} в объекте фильтра отсутствует или указывается как {{calculated}} — бэкенд обрабатывает значение фильтра 
как строку ({{"string"}}) *без* добавления разделителей разрядов и без попытки привести к дате (поведение аналогично текущей реализации, до данной доработки).

h5. 6.6. Итоговый расширенный пример блока {{export.parameters.filter}} в запросе.

{code:json}
"filter": [
  {
    "name": "ID менеджера",
    "values": "[1; 2; 3; 4; 5; 6; 7; 8]",
    "filterType": "number"
  },
  {
    "name": "Год",
    "values": "[2022; 2023]",
    "filterType": "year"
  },
  {
    "name": "Продукция",
    "values": "[Диван]",
    "filterType": "string"
  },
  {
    "name": "Федеральный округ",
    "values": "[Дальневосточный федеральный округ; Приволжский федеральный округ]",
    "filterType": "string"
  },
  {
    "name": "Дата заказа",
    "values": "[2024-01-01; 2024-12-31]",
    "filterType": "date",
    "dateFormat": "DD.MM.YYYY"
  }
]
{code}
