После добавления вычисляемого поля не исчезает выпадающее окно с параметрами вычисляемого поля при нажатии "редактировать". Окно параметров вычисляемого поля должно исчезать в момент нажатия "редактировать", фокус ввода переходит в окно редактирования.


Шаг 1. Базовые настройки тикета
В Jira нажимаем "Создать" (Create) и выбираем:

Проект (Project): ModusBI (или соответствующий проект/доска)

Тип задачи (Issue Type): Ошибка (Bug)

Шаг 2. Заголовок (Summary)
Заголовок должен отвечать на вопросы: Что? Где? Когда/При каком условии? Разработчик должен понять суть бага, просто пробежавшись глазами по бэклогу.

Пишем в Jira:
[Вычисляемые поля] Выпадающее окно параметров не закрывается при клике на "Редактировать"

Шаг 3. Описание (Description)
Это сердце баг-репорта. Используем стандартную структуру: Предусловия, Шаги, Фактический результат (ФР) и Ожидаемый результат (ОР).

Скопируй и вставь этот блок в описание задачи:

Предусловия (Preconditions):

Открыт дашборд/датасет в режиме редактирования.

В рабочей области добавлено {Вычисляемое поле}.

Шаги для воспроизведения (Steps to reproduce):

Кликнуть на добавленное {Вычисляемое поле}, чтобы вызвать выпадающее окно с параметрами (Сортировка, Тип, Уровень вычисления и т.д.).

В самом низу выпадающего окна нажать на кнопку/ссылку "Редактировать".

Фактический результат (Actual Result):
Выпадающее окно с параметрами остается открытым, перекрывая интерфейс ввода формулы. Фокус ввода не устанавливается.

Ожидаемый результат (Expected Result):

Выпадающее окно параметров мгновенно закрывается.

Фокус ввода автоматически переходит в текстовое поле для редактирования формулы (курсор начинает мигать в поле ввода).

Шаг 4. Вложения (Attachments)
Визуал решает. Слова могут трактовать по-разному, скриншот — никогда.

Прикрепи скриншот, который ты мне прислал (он отличный, так как красной рамкой выделена проблемная зона).

Совет от сеньора: Для UI-багов с "залипающими" окнами идеальный вариант — прикрепить короткую GIF-ку или скринкаст на 5-10 секунд. Это снимает 90% вопросов.

Шаг 5. Окружение и Атрибуты
Заполни оставшиеся поля, чтобы тестировщикам и разработчикам было проще локализовать проблему.

Окружение: Стенд: Study, Браузер: Google Chrome (версия 144.0.7559.109 (Official Build) (64-bit)), ОС: Linux Arch / KDE Plasma.

Приоритет (Priority): Minor или Medium (Низкий/Средний). Это не блокер, система не падает, функционал (написание формулы) скорее всего доступен, если кликнуть мышкой мимо окна. Но это явный дефект юзабилити (UX).

Компоненты (Components): Выбери нужный тег из вашей структуры, например, UI, Calculated Fields, Dashboard Editor.

Метки (Labels): ux_bug, front_end.
