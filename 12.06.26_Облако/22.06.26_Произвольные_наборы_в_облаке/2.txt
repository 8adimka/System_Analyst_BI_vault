Ранее была подготовлена постановка ./Внедрение_произвольных_наборов_данных_в_облавном_портале.txt

Необходимо внести исправления и дополнения в текущую постановку:

1. Область применения: On-Premise + условная видимость вкладок.
Дополнение в раздел 2.1 / 3.1:

Новая вкладочная структура («Произвольные наборы данных» + «Простые и составные наборы данных») синхронизируется с On-Premise, но вкладки отображаются условно: если облачный функционал не активирован через метаданные портала, в On-Premise-инсталляции — горизонтальные вкладки не отображаются вообще, открывается только «Произвольные наборы данных» без табов.

Текущая постановка написана только для облака. Необходимо явно зафиксировать, что On-Premise получает аналогичный интерфейс, но с управляемым включением панели вкладок через метаданные.

2. Хлебные крошки: для панели навигации используем интерфейсное решение с вкладки "Наборы данных" для "Произвольных наборов данных" из On-Premise портала.
Уточнение разделов 2.4 и 3.1:

Глобальная унификация хлебных крошек по всему порталу — отдельная задача, не входит в текущую постановку.

В рамках данной задачи: хлебные крошки в разделе «Наборы данных» приводятся к формату как в On-Premise (из существующего компонента). Т.е. для данной вкладки используем уже созданное готовое решение с минимальными доработками.

3. В постановке необходимо явно написать: «использовать компонент хлебных крошек из On-Premise-портала; глобальная переработка хлебных крошек — вне скоупа данной задачи».

4. Иконка в боковой панели — использовать иконку вкладки "Наборы данных" (для «Произвольных наборов данных) из On-Premise-портала.
 
5. Дополнение в раздел 4 «Пограничные сценарии»: Добавить сценарий: «облачный функционал не активирован на On-Premise портале» — вкладочная панель скрыта, раздел «Наборы данных» открывается сразу в режиме «Произвольных наборов данных» без табов, поведение идентично текущему On-Premise-поведению.

6. Явно указать разработчикам: за основу формы «Произвольные наборы данных» берётся существующий On-Premise-компонент, а не создаётся с нуля из облачного кода. Это было проговорено устно, но в постановке не зафиксировано.

Как системный аналитик с большим стажем, проанализируй текущую постановку и необходимые дополнения и пояснения, которые я указал. Минимальными изменениями в исходном тексте внеси все необходимые исправления и дополнения, чтобы постановка стала полностью достаточной, учитывала все необходимые доработки, давала разработчикам достаточно информации для корректной реализации данной доработки и полностью согласовывалась сама с собой.

Финальная постановка должна учитывать все 6 описанных мной исправлений и дополнений и полностью с ними согласовываться по содержанию.

Финальный текст постановки должен быть понятным разработчикам, достаточным, однозначным, талантливо-написанным и включать все необходимые разделы, включая критерии приёмки (AC). При написании постановки используй оптимальную разметку для публикации в Jira, схожую с разметкой из уже реализованной постановкой.

Постановка должна декларативно давать разработчикам информацию о необходимых доработках нашего портала, разработчики не должны касаться изменений внесенных в постановку, дополнений, уточнений и прочих некритичных деталяей в рамках реализации данной постановки.

Как системный аналитик с большим стажем на основе новых вводных и требуемых исправлений составь финальную версию постановки.

Финальную версию постановки запиши в файл ./Внедрение_произвольных_наборов_данных_в_облавном_портале2.txt


