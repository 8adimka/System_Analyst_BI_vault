Как грамотный системный аналитик с большим стажем, который работает над доработкой функциональности аналитического портала Modus BI, подготовь постановку для разрабтчиком для решения имеющихся проблем и недоработок в выпадающем списке для фильтров, добавленных на полку "Фильтры" в визуализациях. Также данная добаботка должна касаться аналогичных настроек из правой общей панели фильтров.

Для подготовки данной постановки тебе понадобится информация о портале Modus BI, часть информации я дам тебе, а остальное найди в Интернете по порталу Modus BI.

Полка «Фильтры» используется для размещения полей, которые будут использованы в качестве фильтров для настраиваемого компонента. Для того чтобы выбрать фильтры, необходимо перенести поле набора данных на полку «Фильтры».

Примечания:
1) Локальный фильтр — это фильтр, который работает только в одной отдельно взятой визуализации. Например, если настроить в диаграмме локальный фильтр по значению «Республика Татарстан», то данная визуализация будет отображать значения по данному региону, игнорируя остальные фильтры. Локальный фильтр при необходимости управляется через боковую панель фильтров.
2) Общий фильтр — это фильтр, который потенциально может влиять на все визуализации в отчете, где этот фильтр присутствует (т.е добавлен).
Если необходимо настроить локальный фильтр для одного визуального компонента, то:

выберите компонент, и для фильтра с полки "Фильтры" включите тумблер "Локальный фильтр" для этого фильтра или в боковой панеле фильтров настройте локальные фильтры на конкретные выбранные фильтры;

В интерфейсе настройки фильтров есть следующие опции:

«Имя поля» — заполните имя поля;
«Уровень отбора»  — заполните значение. Поле позволяет выбрать на каком уровне сработает фильтрация в многоуровневых SQL-запросах. Настройка уровня отбора может быть необходима в ситуациях, когда, например, требуется чтобы на одном визуальном компоненте для одного показателя набора данных применялся один фильтр, а для другого показателя набора данных применялся другой фильтр. В таком случае у показателя набора данных, размещённого на полке «Значения» необходимо установить соответсвующий уровень вычисления;
«Выпадающий список значений» — из выпадающего списка значений поля данных можно выбрать одно или несколько значений, которые будут выбраны по умолчанию;
«Тип фильтра» — заполните значение из выпадающего фильтра, поле необходимо для выборки данных по значению/ диапазону/ сравнению;
«Автофильтр» —  при изменении данных позволяет автоматически выбирать минимальное или максимальное значение;
«Зависит от» — поле позволяет настроить список выводимых значений, который будет зависеть от значений другого общего фильтра в этом отчете (фильтра-родителя), т.е. после выбора значения в фильтре-родителе список значений подчиненного фильтра будет предварительно отфильтрован. Зависимость можно установить как от одного фильтра, так и от нескольких;
«Сброс» — переключатель позволяет включить или отключить функцию сброса значений зависимого фильтра при изменении значений фильтра-родителя. Если опция сброса отключена, то пользователю придется контролировать, что значения в фильтре-родителе и подчиненном фильтре не будут взаимоисключающими;
«Значение при сбросе» — опция работает при включенной настройке «Сброс». При изменении настройки фильтра-родителя будут сброшены значения зависимого фильтра и либо не выбрано значение (вариант «все значения»), либо установлено минимальное (первое в списке по возрастанию) из подходящих значений (этот вариант используется, если в фильтре обязательно должно быть установлено какое-то значение);
«Сортировка по другому полю» — заполните поле из выпадающего списка. Поле необходимо заполнить, например, для периода, когда в фильтре выводится название периода, а сортировка производится по дате начала периода;
«Невидимый» — переключатель, который позволяет включить/ отключить функцию отображения данного фильтра при настройке общих фильтров отчёта;
«Локальный» — переключатель, который позволяет включить/ отключить функцию фильтрации по значениям общего фильтра. Если опция локального фильтра включена, то изменения общего фильтра по выбранному полю не будут затрагивать данный визуальный компонент, благодаря этому, для отдельно выбранного компонента можно применить фильтрацию, которая не будет затрагивать другие компоненты.

Также есть транскрипция разговора с коллегой по данной постановке и текущих проблемах - "".

Начал писать Черновую постановку - 
"

".

На основе транскрипции, где мы подсветили текущие проблемы, и моей черновой постановки, как системный аналитик с большим опытом, напиши полную, качественную и глубокую постановку данной задачи для разработчика в полностью готовом для публикации в Jira виде, чтобы разработчик мог исправить все обозначенные проблемы. Для этого проанализируй текущую постановку, дополни её или исправь (при необходимости) и уже на основе текущей черновой постановки сформулируй полную и понятную постановку для разработчика, опиши какие доработки требуются на фронтенде и какие изменения должны быть внесены на бэкенд, чтобы обеспечить чёткую и стабильную работу фильтров и фильтрации данных по ним. В итоговую постановку включи все необходимые разделы, включая "Критерии приёмки (AC)" и прочие необходимые разделы, чтобы постановка была полной, однозначной и достаточной. Используй и доработай разметку в аналогичном стиле, совместимом с Jira.

