Как грамотный системный аналитик с большим стажем, который работает над доработкой функциональности аналитического портала Modus BI, подготовь постановку для разрабтчиком для решения имеющихся проблем и недоработок в выпадающем списке для фильтров, добавленных на полку "Фильтры" в визуализациях. Также данная добаботка должна касаться аналогичных настроек из правой общей панели фильтров.

Для подготовки данной постановки тебе понадобится информация о портале Modus BI, часть информации я дам тебе, а остальное найди в Интернете по порталу Modus BI.

Полка «Фильтры» используется для размещения полей, которые будут использованы в качестве фильтров для настраиваемого компонента. Для того чтобы выбрать фильтры, необходимо перенести поле набора данных на полку «Фильтры».

Примечания:
1) Локальный фильтр — это фильтр, который работает только в одной отдельно взятой визуализации. Например, если настроить в диаграмме локальный фильтр по значению «Республика Татарстан», то данная визуализация будет отображать значения по данному региону, игнорируя остальные фильтры. Локальный фильтр при необходимости управляется через боковую панель фильтров.
2) Общий фильтр — это фильтр, который потенциально может влиять на все визуализации в отчете, где этот фильтр присутствует (т.е добавлен).
Если необходимо настроить локальный фильтр для одного визуального компонента, то:

выберите компонент, и для фильтра с полки "Фильтры" включите тумблер "Локальный фильтр" для этого фильтра или в боковой панеле фильтров настройте локальные фильтры на конкретные выбранные фильтры;

В интерфейсе настройки фильтров есть следующие опции:

«Имя поля» — заполните имя поля;
«Уровень отбора»  — заполните значение. Поле позволяет выбрать на каком уровне сработает фильтрация в многоуровневых SQL-запросах. Настройка уровня отбора может быть необходима в ситуациях, когда, например, требуется чтобы на одном визуальном компоненте для одного показателя набора данных применялся один фильтр, а для другого показателя набора данных применялся другой фильтр. В таком случае у показателя набора данных, размещённого на полке «Значения» необходимо установить соответсвующий уровень вычисления;
«Выпадающий список значений» — из выпадающего списка значений поля данных можно выбрать одно или несколько значений, которые будут выбраны по умолчанию;
«Тип фильтра» — заполните значение из выпадающего фильтра, поле необходимо для выборки данных по значению/ диапазону/ сравнению;
«Автофильтр» —  при изменении данных позволяет автоматически выбирать минимальное или максимальное значение;
«Зависит от» — поле позволяет настроить список выводимых значений, который будет зависеть от значений другого общего фильтра в этом отчете (фильтра-родителя), т.е. после выбора значения в фильтре-родителе список значений подчиненного фильтра будет предварительно отфильтрован. Зависимость можно установить как от одного фильтра, так и от нескольких;
«Сброс» — переключатель позволяет включить или отключить функцию сброса значений зависимого фильтра при изменении значений фильтра-родителя. Если опция сброса отключена, то пользователю придется контролировать, что значения в фильтре-родителе и подчиненном фильтре не будут взаимоисключающими;
«Значение при сбросе» — опция работает при включенной настройке «Сброс». При изменении настройки фильтра-родителя будут сброшены значения зависимого фильтра и либо не выбрано значение (вариант «все значения»), либо установлено минимальное (первое в списке по возрастанию) из подходящих значений (этот вариант используется, если в фильтре обязательно должно быть установлено какое-то значение);
«Сортировка по другому полю» — заполните поле из выпадающего списка. Поле необходимо заполнить, например, для периода, когда в фильтре выводится название периода, а сортировка производится по дате начала периода;
«Невидимый» — переключатель, который позволяет включить/ отключить функцию отображения данного фильтра при настройке общих фильтров отчёта;
«Локальный» — переключатель, который позволяет включить/ отключить функцию фильтрации по значениям общего фильтра. Если опция локального фильтра включена, то изменения общего фильтра по выбранному полю не будут затрагивать данный визуальный компонент, благодаря этому, для отдельно выбранного компонента можно применить фильтрацию, которая не будет затрагивать другие компоненты.

Также есть транскрипция разговора с коллегой по данной постановке и текущих проблемах - "".

Начал писать Черновую постановку - 
"
Доработка логики и исправление дефектов настроек фильтров

h3. Контекст и описание.
Необходимо провести рефакторинг и исправление ошибок в интерфейсе настройки фильтров. 
Изменения затрагивают меню настроек поля, добавленного на *полку «Фильтры»* (внутри визуального компонента), а также аналогичные настройки в *общей (правой боковой) панели фильтров*.

Текущая реализация содержит ряд дефектов, связанных с обновлением состояния визуализаций, некорректным отображением зависимых полей и сбоями при отображении типа фильтра. 

Цель задачи — стабилизировать работу фильтров, улучшить UX за счет скрытия неактуальных настроек и обеспечить корректную фильтрацию данных.

----
h3. Текущее поведение (As-Is):
# *UX/UI зависимых фильтров:* Поля «Сброс» и «Значение при сбросе» отображаются всегда и доступны для редактирования даже в том случае, если поле «Зависит от» (фильтр-родитель) *не заполнено*. Это запутывает пользователя и позволяет задать настройки (в боковой панеле), которые не должны работать, без заполнения поля «Зависит от».
# *Пустой список «Зависит от»:* При наличии нескольких полей на полке «Фильтры», выпадающий список в настройке «Зависит от» остается пустым (не подтягивает доступные для связи поля).
# *Задержка отработки «Автофильтра»:* В визуализации типа «Таблица» при включении «Автофильтра» ({{MIN}}/{{MAX}}) данные на фронтенде не обновляются сразу в режиме редактирования. Таблица отображает все записи, и корректная фильтрация применяется только после выхода из режима редактирования.
# *Ошибка «Типа фильтра»:* Наблюдается баг: при использовании "вычисляемого поля" в качестве фильтра, в настройках в боковой панели фильтров он отображается как тип фильтра «По диапазону», однако, при закрытии настроек, фильтр отображается как тип «По значению» (выпадающий список).

----
h3. Ожидаемое поведение (To-Be):
# *Динамический UI зависимостей:* Настройки {{Сброс}} и {{Значение при сбросе}} скрыты по умолчанию. Они появляются только после того, как в поле {{Зависит от}} выбрано конкретное значение.
# *Контекст полей:* Выпадающий список {{Зависит от}} корректно собирает и отображает все другие поля, добавленные на полку «Фильтры» (или в боковой панели), исключая само себя.
# *Мгновенный рендер (Автофильтр):* Изменение настройки {{Автофильтр}} в таблице инициирует немедленный запрос данных и перерендер компонента прямо в режиме редактирования.
# *Стабильность состояния:* Выбранный {{Тип фильтра}} жестко фиксируется в стейте и одинакого отображается как в настройках, так и в рабочем режиме. Закрытие/открытие настроек не меняет выбранный тип отображения фильтра, включая для фильтров, созданных с использованием "вычисляемых полей".
# *Корректная работа SQL-диапазонов:* Фильтрация {{По диапазону}} отрабатывает корректно, включая *пограничные значения*, данные на графиках/таблицах отображаются штатно.
# *Динамическая сортировка:* (_опционально, работает не на всех версиях портала_) Поле {{Направление сортировки}} скрывается, если не заполнено поле {{Сортировка по другому полю}}.

----
h3. Требования:

{panel:title=Frontend|borderStyle=solid|borderColor=#36B37E|titleBGColor=#E3FCEF|bgColor=#F4F5F7}
*Условная видимость полей:*
* Поля «Сброс» и «Значение при сбросе» должны *исключаться из DOM* (не рендериться), пока поле «Зависит от» не содержит хотя бы одного выбранного значения.
* При очистке «Зависит от» — оба поля скрываются, их текущие значения сбрасываются в дефолт.
* Аналогичную логику применить к полю «Направление сортировки»: скрывать из DOM, пока не заполнено поле «Сортировка по другому полю».

*Список значений «Зависит от»:*
* Выпадающий список формируется из всех полей-фильтров текущего контекста (полка «Фильтры» виджета {{+}} боковая панель), *исключая само редактируемое поле*.
* Список обновляется реактивно при добавлении или удалении полей.
* Если доступных полей-родителей нет — поле «Зависит от» переводится в состояние *disabled* с соответствующим сообщением — "Нет доступных полей".

*Реактивность Автофильтра в табличном компоненте:*
* При изменении настройки «Автофильтр» компонент должен *немедленно инициировать запрос за данными и перерисовку* — прямо в режиме редактирования, не ожидая сохранения или выхода из режима редактирования.
* *Место внесения изменений*: если есть единый механизм отслеживания изменений настроек фильтра - используем его, если нет - для табличного компонента используется отдельный.

*Сохранение и верное отображение «Типа фильтра»:*
* Значение поля «Тип фильтра» должно сохраняться (в том числе при его изменении пользователем)* и отрисовываться в одинаковом виде в общей панели фильтра в режиме настроек и в режиме использования панели.
* При повторном открытии настроек компонент инициализируется из *сохранённого* значения, а не из значения по умолчанию.
* Закрытие окна настроек не должно влиять на уже сохранённое значение.
{panel}

{panel:title=Backend|borderStyle=solid|borderColor=#0052CC|titleBGColor=#DEEBFF|bgColor=#F4F5F7}
*Фильтрация «По диапазону» — генерация SQL:*
* Убедиться, что для диапазонного фильтра генерируется SQL-условие с *инклюзивными* границами (значения на границах включаются в выборку), если это задано в настройках фильтра.
* *Приведение типов:* значения, переданные из UI, должны приводиться к типу целевой колонки на основе её метаданных *до подстановки в запрос*. 
Строковое сравнение числовых значений — вероятная причина пустой выборки.
* При незаполненной одной из границ — генерировать одностороннее условие (только нижняя или только верхняя граница).
Пустая граница не должна приводить к некорректному запросу.

*Автофильтр — корректность агрегации:*
* При расчёте MIN/MAX для автофильтра убедиться, что агрегация выполняется с учётом заданного параметра «Уровень отбора».
{panel}

----
h3. Критерии приёмки (AC):

*AC 1: Динамический UI для зависимых фильтров*
* *Дано* Пользователь открывает настройки поля на полке «Фильтры» (или в Общей панели фильтров), поле {{Зависит от}} - пустое.
* *Когда* Пользователь просматривает список настроек.
* *Тогда* Поля {{Сброс}} и {{Значение при сбросе}} *отсутствуют* в интерфейсе настройки.
* *Когда* Пользователь выбирает любое значение в поле {{Зависит от}}.
* *Тогда* Поля {{Сброс}} и {{Значение при сбросе}} появляются под полем {{Зависит от}} и доступны для редактирования.

*AC 2: Формирование списка для зависимости*
* *Дано* На полке «Фильтры» лежат три поля: "Регион", "Город", "Федеральный округ", иные фильтры в дашборде *не настроены*.
* *Когда* Пользователь открывает настройки поля "Город" и кликает на список {{Зависит от}}.
* *Тогда* В выпадающем списке отображаются только "Регион" и "Федеральный округ". Само поле "Город" отсутствует.

*AC 3: Автофильтр в таблицах*
* *Дано* Пользователь находится в режиме редактирования дашборда. Настроен виджет «Таблица» с добавленным фильтром.
* *Когда* Пользователь включает {{Автофильтр}} для данного фильтра (например, на «Максимальное значение»).
* *Тогда* Фронтенд немедленно отправляет запрос за данными, и таблица перерисовывается с отфильтрованными данными *без* выхода из режима редактирования.

*AC 4: «Типа фильтра» для «Вычисляемого поля»*
* *Дано* Открыты настройки любого "вычисляемого" фильтра ("вычисляемого поля" используемого как фильтр).
* *Когда* Пользователь видит {{Тип фильтра}} и связанные настройки, меняет их и закрывает окно настроек.
* *Тогда* {{Тип фильтра}} соответствует настройкам, фильтрация работает штатно и не происходит отката на "выпадающий список".

*AC 5: Валидность выборки данных по диапазону*
* *Дано* В БД существуют записи со значениями показателя от 100 до 3000.
* *Когда* Пользователь устанавливает {{Тип фильтра}} -> {{По диапазону}}, задает значения от 500 до 2000.
* *Тогда* Визуализация корректно отображает данные. SQL-запрос на бэкенде использует инклюзивные границы, проблем с несовпадением типов данных (String vs Int) не возникает.

*AC 6: Синхронность поведения (Регресс)*
* *Дано* Выполнены доработки по AC 1-5.
* *Когда* Пользователь настраивает фильтры в *общей панели фильтров (справа)*.
* *Тогда* Вся логика отображения полей, выбора зависимостей и сохранения стейта работает абсолютно идентично поведению на полке «Фильтры» внутри виджета.
".

На основе транскрипции, где мы подсветили текущие проблемы, и моей черновой постановки, как системный аналитик с большим опытом, напиши полную, качественную и глубокую постановку данной задачи для разработчика в полностью готовом для публикации в Jira виде, чтобы разработчик мог исправить все обозначенные проблемы. Для этого проанализируй текущую постановку, дополни её или исправь (при необходимости) и уже на основе текущей черновой постановки сформулируй полную и понятную постановку для разработчика, опиши какие доработки требуются на фронтенде и какие изменения должны быть внесены на бэкенд, чтобы обеспечить чёткую и стабильную работу фильтров и фильтрации данных по ним. В итоговую постановку включи все необходимые разделы, включая "Критерии приёмки (AC)" и прочие необходимые разделы, чтобы постановка была полной, однозначной и достаточной. Используй и доработай разметку в аналогичном стиле, совместимом с Jira.

