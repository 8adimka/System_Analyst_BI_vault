h3. Контекст и проблематика.
Наша платформа предоставляет аналитикам возможность строить визуализации на основе _наборов данных трёх типов_: *произвольного* (обычный «пользовательский» SQL-запрос), *простого* или *составного*.
На сегодняшний день все вычисляемые выражения существуют исключительно в контексте конкретной визуализации — как «вычисляемые поля» виджета. Это порождает ряд системных ограничений: 
* Одна и та же бизнес-логика дублируется в каждой визуализации, где она нужна, что ведёт к рассинхронизации определений при изменениях.
* Не существует возможности вынести агрегирующее вычисление на уровень набора данных — то есть определить его один раз и использовать в любых визуализациях, основанных на этом наборе данных.
* Аналитик лишён семантически выразительного инструмента: он вынужден либо дублировать формулы, либо включать агрегации непосредственно в основной SQL набора данных, теряя гибкость.

В индустриальных BI-системах данная потребность закрывается концепцией «Мер» — именованных вычислимых выражений, определённых на уровне источника данных и применяемых при построении визуализации.

----
h3. Цель и бизнес-ценность.
+Этап 1+: Меры реализуются только для *произвольного* (обычного) набора данных — типа набора с произвольным SQL-запросом. Для *простых* и *составных* наборов данных механизм добавления Мер не реализуется в рамках текущей доработки.

Реализация Мер в рамках *произвольного* набора данных позволит:
* Аналитикам определять именованные вычислимые показатели один раз на уровне набора данных и переиспользовать их в любых визуализациях.
* Бэкенду корректно встраивать выражение Меры в нужную SQL-клаузу внешнего запроса в зависимости от полки размещения, используя текущий механизм построителя запросов (базовый слой запроса).
* Поддержать полный спектр сценариев использования: от агрегации значений и фильтрации по агрегатам до вычисляемых измерений и тултип-метрик.
* Устранить дублирование бизнес-логики и обеспечить единую точку изменения вычислений.
* Переиспользовать Меры в выражении друг друга и в вычисляемых полях, что значительно сократит сложность чтения формул.
* Заложить основу для последующего расширения Мер на _простые_ и _составные_ наборы данных (+Этап 2+ — за пределами настоящей постановки).

----
h3. Ключевые понятия и определения в рамках постановки.
* +Мера+ — именованное вычислимое выражение, определённое на уровне +произвольного+ набора данных. Мера представляет собой SQL-выражение, при написании которого аналитиком могут использоваться поля данного набора данных, ранее определённые Меры и глобальные переменные. Сама по себе Мера не вычисляется в момент создания; она вычисляется динамически в контексте конкретной визуализации — в зависимости от полки, на которую она размещена, её выражение инжектируется в строго определённую SQL-клаузу внешнего запроса. Основой всех внешних ссылок на Меру (из визуализаций, настроек, формул других Мер и Вычисляемых полей) является +уникальное имя Меры ({{name}}) в рамках одного набора данных+. При *переименовании*, Мера теряет связь со всеми связанными объектами, а в случае появления новой Меры с тем же именем (как до переименования) - она автоматически займёт её место по уже существующим связям.
* +Удалённая Мера+ (аттрибут {{deleted}} = 1) — Мера, получившая отметку об удалении. Физически не удаляется из метаданных набора данных, но исчезает из всех пользовательских интерфейсов и панелей выбора полей. Ссылки на удалённую Меру в визуализациях и настройках визуализаций, а также Меры, использующие удалённую Меру в формулах, становятся +невалидными+.
* +Агрегация+ ({{aggregation_kind}}) — атрибут Меры, указывающий, содержит ли её выражение агрегатные функции. Задаётся пользователем при создании или редактировании: {{1}} — да (мера агрегированная), {{0}} — нет (неагрегированная). Используется для валидации размещения мер на полках и для визуального обозначения (ярлык *AGR* для агрегированных мер). 
* +Мера считается агригированной+ ({{aggregation_kind = 1}}), если её выражение содержит хотя бы одну агрегатную функцию: {{SUM()}}, {{AVG()}}, {{MAX()}}, {{COUNT(DISTINCT...)}} и их комбинации. Допустима только в {{SELECT}} и {{HAVING}}, но не в {{WHERE}} и {{GROUP BY}}.
* +Мера считается неагрегированной+ ({{aggregation_kind = 0}}), если её выражение не содержит агрегатных функций; работает на уровне отдельных строк. Допустима в {{SELECT}}, {{WHERE}}, {{GROUP BY}}.
* +Базовый слой запроса+ — текущий механизм построителя, в котором оригинальный SQL набора данных используется как источник внешнего запроса визуализации. Реализация допускает два эквивалентных варианта: {{FROM (<dataset_sql>) AS __base}} (derived table) или {{WITH __base AS (<dataset_sql>) ... SELECT ... FROM __base}} (CTE). Выбор между ними является технической деталью и не фиксируется как продуктовое требование.
* +Полка+ — именованная область в конструкторе визуализации, на которую аналитик размещает поля или Меры методом drag-and-drop. Конкретный набор полок зависит от типа визуализации.

----
h3. Пользовательский интерфейс и управление Мерами в настройках Набора данных.
В настройках аналитического портала, во вкладке «Наборы данных» мы выбираем «Источник данных» и +произвольный+ «Набор данных».
Под секцией «Описание полей набора данных» добавляется новая секция «Меры».
Для +простых+ и +составных+ наборов данных секция «Меры» и кнопка «+ Добавить меру» **не отображаются** (функциональность не реализуется в рамках +Этапа 1+).

h4. Доступность кнопки «+ Добавить меру».
Кнопка «+ Добавить меру» активна *только* при одновременном выполнении всех условий:
# для набора задан SQL-запрос;
# запрос успешно прошёл проверку;
# система получила актуальный состав полей набора данных.

Если хотя бы одно условие не выполнено — кнопка находится в {{disabled}}-состоянии. При наведении на неё отображается пояснение: _«Задайте и проверьте SQL-запрос набора данных»_. Если SQL-запрос отсутствует или не проходит проверку, кнопка «+ Добавить меру» остаётся {{disabled}}, но ранее сохранённые Меры *не удаляются* — их повторная валидация откладывается до появления корректного SQL и актуального состава полей. 

h4. Интерфейс создания и редактирования Меры.
При нажатии «+ Добавить меру» открывается *модальное окно увеличенного размера*.

Внутри модального окна одновременно доступны:
* поля формы Меры (левая или верхняя часть);
* редактор выражения со списком полей текущего набора данных и ранее созданных Мер (правая или нижняя часть) — элементы доступны для вставки в выражение непосредственно из модалки, так как основная страница с полями перекрыта окном.

При нажатии «Редактировать» на существующей Мере открывается то же окно с предзаполненными значениями.

Форма содержит следующие поля:
* *Название* (обязательное, строка) — отображаемое имя Меры; должно быть уникальным в рамках одного набора данных.
* *Описание* (необязательное, строка) — пояснительный текст; отображается в tooltip при наведении на Меру.
* *Тип данных* (обязательное) — логический тип результата Меры: {{number}}, {{string}}, {{date}}, {{datetime}} и т.д. Система может предложить значение по умолчанию, но пользователь обязан иметь возможность изменить тип вручную. Если для выбранного типа доступны дополнительные настройки (например, для дат), они отображаются условно.
* *Агрегация* (обязательное, переключатель/флаг) — задаётся пользователем явно: {{да (1)}} / {{нет (0)}}. Используется для UX-валидации при размещении на полки.
* *Иерархия* (необязательное, строка) — имя группы/иерархии, в которой Мера отображается в панели полей.
* *Уровень* (необязательное, целое число) — порядок отображения Меры внутри иерархии.
* *Выражение* (обязательное) — редактор выражений, эквивалентный редактору вычисляемых полей виджета. Требования к редактору:
** Синтаксис и функции — идентичны вычисляемым полям.
** В редакторе доступен список полей текущего набора данных и ранее созданных активных ({{deleted = 0}}) Мер.
** Обычные поля набора данных оборачиваются в *двойные кавычки*: {{"field_name"}}; Меры оборачиваются в квадратные скобки: {{[measure_name]}}. Это визуально разграничивает обращения к полям и к Мерам в теле формулы.
** Автодополнение должно различать и предлагать отдельно: поля набора данных и Меры. В выпадающем списке автодополнения Меры визуально отличаются от полей (например, цветом, значком или секцией).
** Подсветка синтаксиса должна отличать в квадратные скобки (ссылки на Меры) от двойных (ссылки на поля) — разными цветами.
** Подсветка синтаксических ошибок и другие продвинутые возможности нового редактора кода.
* *Кнопки формы:* «Сохранить» / «Отмена».

h4. Валидация при сохранении Меры.

При сохранении выражения Меры система выполняет:
* Синтаксическую проверку выражения;
* Проверку существование используемых полей текущего набора данных;
** Если выражение Меры *не содержит ссылок на поля набора данных* (например, использует только глобальные переменные или константы), то отсутствие полей не делает её автоматически невалидной ({{invalid}}) — при корректном синтаксисе такая Мера остаётся валидной ({{valid}});
* Проверку существования активных Мер (deleted = 0), на которые есть ссылки в формате [measure_name];
* Проверку отсутствия самоссылок и циклических зависимостей;
** При обнаружении цикла сохранение блокируется с ошибкой:
«Выражение Меры содержит циклическую зависимость»;
* Проверку существования используемых глобальных переменных.

При сохранении Меры выполняются следующие проверки:
* Сохранено валидное выражение Меры;
* При редактировании уже существующей Меры с изменением её наименования, если система обнаруживает, что Мера с прежним именем имеет зависимые объекты (визуализации, другие Меры и т.д.), то в момент сохранения отображается дополнительное предупреждение по форме:
*«Мера "[Название]" используется в [N] визуализациях и/или в выражениях других Мер. Переименование Меры может вызвать ошибки в связанных объектах. Продолжить?»*
** Кнопка «Сохранить» остаётся доступной — пользователь может подтвердить или отменить переименование.
** Если зависимых объектов нет — дополнительное предупреждение не отображается, сохранение происходит штатно.
** Изменение других полей формы (описание, тип данных, выражение и т.д.) не вызывает дополнительного предупреждения.

При успешном сохранении:
* новая Мера получает стабильный {{id}};
* Мера сохраняется в {{метаданные}} набора данных;
* в случае прохождения всех проверок, для Меры устанавливается статус {{valid}}.
* в случае непрохождения одной или нескольких проверок, после сохранения Мера получает статус {{invalid}}:
** если Мера получает статус невалидной ({{invalid}}) или удалённой ({{deleted=1}}), то все Меры и Вычисляемые поля, использующие эту меру в своих выражениях, также получают невалидный статус ({{invalid}}).

h4. Валидация Мер при изменении других Мер.

Помимо ревалидации при изменении SQL-запроса, система выполняет ревалидацию зависимых Мер при *переименовании или мягком удалении* любой Меры данного набора данных.

*Алгоритм:*
# При переименовании Меры A ({{name_old}} → {{name_new}}) или при её мягком удалении ({{deleted = 1}}) система обходит все остальные Меры того же набора данных.
# Для каждой Меры, в выражении которой содержится ссылка {{'name_old'}}, присваивается статус {{invalid}} с причиной: *«Мера {{[name_old]}}, на которую ссылается выражение, была переименована или удалена»*, т.к. ссылка становится неразрешимой (нет активной Меры с именем {{name_old}}).
# Если впоследствии создаётся новая Мера с именем {{name_old}} (или, при переименовании, возвращается исходное имя) или же удалённая Мера восстанавливается, система выполняет ревалидацию Мер — при синтаксически корректном выражении они получают статус {{valid}}.

*Сводная таблица триггеров ревалидации Меры:*

| Событие | Какие Меры ревалидируются | Критерий проверки |
|---|---|---|
| Изменение SQL набора данных | Все Меры, ссылающиеся на поля Набора данных | Поле из выражения отсутствует в актуальном составе Набора |
| Переименование или мягкое удаление Меры A | Все Меры, ссылающиеся на прежнее имя Меры A | Ссылка {{[old_name]}} не разрешается в активную ({{deleted=0}}) Меру |
| Восстановление Меры / создание Меры с тем же именем | Все Меры со статусом {{invalid}} | Ссылка вновь разрешается в активную валидную Меру |

h4. Список Мер набора данных.
Сохранённые Меры отображаются в *табличном списке* под секцией «Описание полей набора данных».

Набор отображаемых колонок:

|| *Название* || *Тип данных* || *Агрегация* || *Иерархия* || *Уровень* || *Выражение* (кратко) || *Действия* ||
| <название меры> | <тип> | AGR / — | <иерархия> | <порядок> | <первые N символов> | [иконка редактировать] [иконка удалить] |

* В столбце «Выражение» показываются только первые {{N}} символов формулы. Полный текст записанной формулы доступен при наведении на сокращённое выражение (tooltip).
* Описание Меры выводится во всплывающей подсказке (tooltip) при наведении на её строку.
* Колонка *«Агрегация»* — для Мер с {{aggregation_kind = 1}} отображается ярлык {{AGR}}; для {{aggregation_kind = 0}} — прочерк ("—").
* Для Мер со статусом {{invalid}} строка подсвечивается как ошибочная (красный ореол, иконка ⚠). При наведении отображается причина невалидности.
* Если заполнена «Иерархия», Мера относится к соответствующей display-группе.
* Внутри одной группы Меры сортируются по {{level}} по возрастанию, затем по названию.
* Меры без заполненной «Иерархии» отображаются в корневом уровне секции «Меры».

* По умолчанию в списке отображаются только активные Меры (deleted = 0). При включении тумблера *«Отображать удалённые Меры»* в список добавляются удалённые Меры (deleted = 1) с визуальным выделением и кнопкой «Восстановить» вместо стандартных действий (см. раздел «Просмотр и восстановление удалённых Мер»).

h4. Удаление Меры (мягкое удаление).

При нажатии иконку удаления Меры система определяет, используется ли Мера в каких-либо визуализациях или формулах других Мер.

*Если Мера не используется* нигде — система запрашивает подтверждение (без предупреждений о зависимостях), после подтверждения Мера получает флаг {{deleted = 1}}.

*Если Мера используется* хотя бы в одной визуализации или в формуле другой Меры — перед удалением отображается предупреждение:
* *«Мера "[Название]" используется в [N] визуализациях и/или в выражениях других Мер. Удаление Меры может вызвать ошибки в связанных объектах. Продолжить?»*
* После подтверждения:
** Мера получает флаг {{deleted = 1}} и *не удаляется физически* из метаданных набора данных.
** Мера исчезает из списка отображаемых Мер в настройках набора данных (кроме случаев активации тумблера "Отображать удалённые Меры").
** Мера не отображается в боковой панеле полей в конструкторе визуализаций.
** Мера не отображается в списке доступных элементов в редакторах формул (вычисляемых полей и других Мер).
** Все визуализации, содержащие эту Меру на полках или в настройках, переходят в состояние ошибки: удалённая Мера подсвечивается в списке как ошибочная (красный ореол, иконка ⚠, при наведении отображается текст ошибки или причина невалидности), запрос данных блокируется с сообщением об ошибке.
** Все Меры, использующие имя удалённой Меры в своей формуле, получают статус {{invalid}}.

*Физическое удаление* записи о Мере из метаданных не выполняется. Это позволяет восстановить удалённую меру (при активации тумблера "Отображать удалённые Меры"). 
* Имя Меры ({{name}}) должно быть уникальным среди всех Мер одного набора данных, включая активные (deleted = 0) и удалённые (deleted = 1). Если пользователь при создании новой Меры вводит имя, совпадающее с именем удалённой Меры, новая запись не создаётся, а существующая удалённая Мера *обновляется* введёнными значениями и получает активный статус ({{deleted = 0}}). Cистема начинает разрешать все имеющиеся ссылки на это *имя* в активную Меру, и ошибочные состояния устраняются, Меры ссылающиеся на данную обновлённую Меру ревалидируются.

*Примечание*: набор данных может быть сохранён даже при наличии Мер с {{deleted = 1}} или {{status = invalid}}.

h4. Просмотр и восстановление удалённых Мер.
В секции «Меры» настроек набора данных над табличным списком располагается тумблер *«Отображать удалённые Меры»* (по умолчанию — выключен).

При выключённом тумблере (состояние по умолчанию):
* В табличном списке отображаются только Меры с {{deleted = 0}} (активные), Меры со статусом {{invalid}} дополнительно подсвечиваются иконкой ⚠.
* Удалённые Меры (deleted = 1) скрыты.

При включённом тумблере:
* В табличном списке дополнительно отображаются Меры с {{deleted = 1}}.
* Удалённые Меры визуально отличаются от активных: строка отображается с пониженной непрозрачностью (затемнение) и меткой «Удалена». Иконку «Удалить» заменяет на иконка/кнопка «Восстановить».
* Удалённые Меры со статусом {{invalid}} дополнительно подсвечиваются иконкой ⚠ (иконка ⚠ и метка «Удалена» отображаются одновременно).

Восстановление Меры (нажатие «Восстановить»):
* Мера получает deleted = 0, возвращается в список активных Мер, система запускает ревалидацию (по актуальному составу полей и доступным Мерам). Все ссылки на это имя в визуализациях и формулах, находившиеся в ошибочном состоянии, разрешаются на восстановленную Меру — их статусы обновляются автоматически.

h4. Жизненный цикл Меры после изменения SQL набора данных.
При каждом сохранении SQL-запроса набора данных система заново определяет актуальный состав полей и *автоматически валидирует все Меры* набора.

* Если выражение Меры не содержит ссылок на поля набора (например, использует только глобальные переменные или константы), то изменения состава полей набора *не влияют* на статус Меры — она остаётся {{valid}} при корректном синтаксисе выражения.
* Если Мера ссылается на отсутствующее поле или её выражение не может быть интерпретировано в контексте текущего набора данных:
** Мера *не удаляется* автоматически;
** Мера получает статус {{invalid}};
** Мера подсвечивается в списке как ошибочная (красный ореол, иконка ⚠);
** При наведении отображается текст ошибки или причина невалидности;
** Мера *недоступна для добавления на полки* до исправления.
** После исправления выражения или восстановления требуемых полей Мера автоматически получает статус {{valid}}.

Если SQL-запрос набора данных очищен или не проходит проверку, кнопка «+ Добавить меру» остаётся {{disabled}} (новые Меры создать нельзя), но уже сохранённые Меры не удаляются; их повторная валидация откладывается до появления корректного SQL. В интерфейсе визуализации такие Меры можно показывать как _недоступные_ для добавления на полки или ошибочные (красный ореол, иконка ⚠) до восстановления валидного контекста набора.
Однако, мы можем редактировать и сохранить набор данных даже, при наличии в нём Мер со статусом {{invalid}}.

----
h3. Структура метаданных Меры.

Мера сохраняется в JSON-метаданных набора данных. Ключом всех внешних ссылок на Меру является поле {{name}} — уникальное имя Меры в рамках одного набора данных.

{code:json}
{
 "measures": [
  {
   "id": "847b1ec3-bbf8-4365-85fb-c4fca2a06e2e",
   "name": "Валовая прибыль",
   "description": "Разница между выручкой и себестоимостью",
   "expression": "SUM("revenue") - SUM("cost")",
   "aggregation_kind": 1,
   "data_type": "number",
   "hierarchy": "Финансовые показатели",
   "level": 10,
   "status": "valid",
   "deleted": 0
  }
 ]
}
{code}

|| Поле || Тип || Описание ||
| {{id}}       | string | Стабильный UUID идентификатор; не изменяется при переименовании, *Не является основой внешних ссылок на Меру* |
| {{name}}      | string | Отображаемое уникальное название в рамках одного набора данных; *является ключом связи* во всех внешних ссылках на Меру: в визуализациях, настройках визуализаций, формулах, используемых при создании вычисляемых полей и других Мер, т.д. |
| {{description}}  | string | Необязательное описание |
| {{expression}}   | string | SQL-выражение; Меры в формулах оборачиваются в квадратные скобки: {{[measure_name]}} |
| {{aggregation_kind}} | int (0/1) | Является ли мера агрегированной; *задаётся пользователем явно* (1 — да, 0 — нет) |
| {{data_type}}   | string | Логический тип результата: {{number}}, {{string}}, {{date}}, {{datetime}} и т.д. |
| {{hierarchy}}   | string | Необязательная группа/иерархия отображения в панели полей |
| {{level}}     | int  | Порядок Меры внутри иерархии |
| {{status}}     | enum  | {{valid}} или {{invalid}}; *вычисляется системой* по результатам валидации |
| `deleted` | int (0/1) | 0 — мера активна; 1 — мера помечена удалённой (мягкое удаление), не отображается в UI, ссылки на неё становятся невалидными |

В метаданных визуализации каждое размещение Меры на полке фиксируется со ссылкой на {{measure.name}} и дополнительными параметрами ({{filter_mode}}, {{condition}}, а для HAVING-фильтра Сводной таблицы — {{apply_mode}} / {{target_dimensions}}).

---- 
h3. Метаданные визуализации и ссылки на Меры.
Во всех метаданных визуализаций связь с Мерой хранится по имени Меры ({{measure_name}}) — аналогично тому, как обычные поля набора данных адресуются по имени поля.

Это означает, что:
- *Переименование Меры* — имя является ключом связи, поэтому переименование разрывает все внешние ссылки на Меру (из визуализаций, настроек визуализаций, формул других Мер и Вычисляемых полей). Затронутые объекты получают статус {{invalid}} или ошибочное состояние (поведение аналогичное удалению Меры). UI должен информировать аналитика о последствиях переименования (количество затронутых визуализаций и Мер) до подтверждения.
- *Создание новой Меры с именем, совпадающим с именем переименованной Меры*, автоматически «восстанавливает» все ссылки — система начинает разрешать имя в новую Меру без каких-либо ручных действий со стороны пользователя.

Каждое использование Меры в визуализации хранит:
* тип сущности {{measure}};
* ссылку на {{measure_name}};
* дополнительные параметры в зависимости от конкретной полки и сценария.

Пример для фильтра по Мере:
{code:json}
{
 "filters": [
   {
     "id": "f9c35f8f-9f52-49a5-a47f-57badace6312",
     "type": "measure",
     "measure_name": "Валовая прибыль",
     "filter_mode": "HAVING",
     "condition": "> 0"
   }
 ]
}
{code}

|| Поле || Тип || Описание ||
| {{type}}    | string  | {{"measure"}} — отличает от вычисляемых полей ({{"calculated"}}) |
| {{measure_name}} | string  | Имя Меры; *является ключом связи* |
| {{filter_mode}} | string  | {{WHERE}} или {{HAVING}} |
| {{condition}}  | string  | Условие фильтрации |

----
h3. Отображение Мер в конструкторе визуализаций.

h4. Боковая панель полей (слева).
При выборе набора данных в конструкторе боковая панель разделена на секции:
* *«Поля»* — обычные поля и вычисляемые поля виджета (текущий вид без изменений).
* *«Меры»* — Меры набора данных (новая секция ниже).

Отображение Мер на боковой панеле полей (слева):
* Для Мер с {{aggregation_kind = 1}} рядом с названием отображается ярлык *AGR*.
* Tooltip при наведении отображает описание (если заполнено), тип данных и полный текст выражения.
* Меры со статусом {{invalid}} отображаются с иконкой ⚠ и недоступны для drag-and-drop на полки.

Отображение Мер на полке визуализации:
* Цвет иконки (пилюли) — *тёмно-оранжевый* (обычные поля — тёмно-синий).
* Для Мер с {{aggregation_kind = 1}} рядом с названием отображается ярлык *AGR*.
* Уже выбранные меры со статусом {{invalid}} отображаются с иконкой ⚠.

h4. Группировка и порядок Мер в конструкторе визуализаций.
* Если заполнена «Иерархия», Мера показывается в соответствующей display-группе внутри секции «Меры».
** Порядок Мер внутри группы определяется полем {{level}}, затем названием (по алфавиту).
* Если «Иерархия» не заполнена, Мера выводится в корневом уровне секции «Меры».

h4. Статус {{invalid}} в UI конструктора.
* Мера со статусом {{invalid}} не удаляется из метаданных автоматически.
* В конструкторе визуализаций такая Мера отображается с предупреждающей иконкой.
* Tooltip по невалидной Мере дополнительно содержит признак ошибки и краткую причину недоступности.
* Невалидную Меру нельзя добавить на полки до исправления выражения либо восстановления требуемых полей набора данных.
* Если существующая визуализация уже содержит ссылку на невалидную Меру:
** привязка по measure_name сохраняется (ссылка на имя Меры не удаляется автоматически);
** пилюля отображается в error-состоянии;
** запрос данных блокируется с понятным сообщением об ошибке до исправления либо удаления Меры из визуализации.

h4. Правила {{drag-and-drop}} и прочие селекторы полей.
Мера перетаскивается на полки так же, как обычное поле. При попытке разместить Меру на несовместимую полку (правила валидации описаны ниже) перетаскивание блокируется: курсор показывает «запрет», подсказка поясняет причину. Одна и та же Мера может быть размещена на нескольких полках одновременно.

* Меры могут использоваться не только на основных полках визуализации, но и во *всех элементах интерфейса*, где пользователь выбирает поля набора данных в текущей конфигурации визуализации, если бизнес-сценарий это допускает. К таким элементам относятся, в частности:
** настройки стилизации и форматирования;
** условное форматирование;
** сценарии расчётов по уровням;
** другие выпадающие списки, работающие с полями визуализации.
* Например, любую меру можно использовать в вычисляемом поле виджета или в условном форматировании так же, как обычное поле набора данных.

h4. Настройка пилюли на полке «Фильтры».
Попав на полку «Фильтры», Мера отображается в виде пилюли *салатового* цвета, аналогично с другими полями. При клике открывается окно настройки фильтра-Меры.

Интерфейс настройки фильтра-Меры аналогичен стандартному интерфейсу фильтра по полю набора данных.

Форма фильтра по Мере содержит:
* тип отбора: {{WHERE}} или {{HAVING}};
* условие фильтрации;
* дополнительные параметры, если они применимы для данного типа визуализации, как при фильтрации обычного поля.

h4. Ярлык AGR на пилюлях и полках.
* Для Мер с {{aggregation_kind = 1}} ярлык *AGR* отображается: в списке Мер набора данных, в боковой панели конструктора, на пилюлях полок.
* Для Мер с {{aggregation_kind = 0}} ярлык не отображается.
* Изменить {{aggregation_kind}} непосредственно на полке *нельзя*; атрибут фиксируется в настройках Меры.

----
h3. Валидация Мер и правила допустимости по полкам.
Валидация выполняется в трёх точках:
* при сохранении Меры;
* при попытке разместить Меру на полку или выбрать её в совместимом селекторе;
* при генерации SQL на бэкенде перед выполнением запроса.

В рамках +этапа 1+ UX-валидация опирается прежде всего на поле {{aggregation_kind}}. Финальная SQL-валидация выполняется на бэкенде. Если фактическое выражение Меры не соответствует выбранному {{aggregation_kind}}, либо нарушает SQL-семантику итогового запроса, запрос отклоняется с понятной ошибкой.

UX-валидация при размещении на полку опирается на атрибут {{aggregation_kind}} Меры.

h4. Правила валидации по полкам.

|| Полка        || Допустимый тип Меры     || Блокировать при нарушении?                  || Сообщение ||
| *Значения*      | Любая           | Нет блокировки; предупреждение при {{aggregation_kind = 0}} | _«Выражение помечено как неагрегированное. Для корректного отображения рекомендуется использовать агрегатную функцию, например: SUM(...)»_ | 
| *Фильтры (HAVING)*  | Только агрегированная ({{aggregation_kind = 1}})   | Да | _«Мера помечена как неагрегированная. Выберите тип отбора WHERE или измените агрегатность в настройках Меры»_ | 
| *Фильтры (WHERE)*  | Только неагрегированная ({{aggregation_kind = 0}})  | Да | _«Мера помечена как агрегированная. Выберите тип отбора HAVING или измените агрегатность в настройках Меры»_ | 
| *Строки / Столбцы*  | Только неагрегированная ({{aggregation_kind = 0}})  | Да | _«На полках "Строки" и "Столбцы" допустимы только неагрегированные Меры»_ | 
| *Детализация*    | Только неагрегированная ({{aggregation_kind = 0}})  | Да | _«На полке "Детализация" допустимы только неагрегированные Меры»_ | 
| *Дополнительные*  | Любая           | Нет блокировки;  | -|

h4. Дополнительные правила.
* Невалидную Меру ({{status = invalid}}) нельзя разместить ни на одну полку.
* Если Мера была добавлена на полку ранее, а затем перестала быть валидной, визуализация должна явно показывать ошибку, а не удалять такую Меру.
* Меры с полки «Фильтры» объединяются через {{AND}} внутри соответствующей клаузулы.
* Одновременное наличие WHERE- и HAVING-фильтров по Мерам допустимо.

---- 
h3. Логика генерации SQL.

h4. Базовый слой запроса и алгоритм генерации.

Алгоритм выполняется каждый раз при формировании запроса данных для визуализации. Движок всегда строит внешний запрос над базовым слоем: исходный SQL набора данных подставляется без изменений как внутренний подзапрос. Затем в результирующий запрос инжектируются выражения Мер в соответствующие SQL-клаузы: {{SELECT}}, {{WHERE}}, {{GROUP BY}}, {{HAVING}} — в зависимости от полок, на которые они добавлены. Если мер на полках нет, внешний запрос эквивалентен исходному (результат запроса совпадает с текущей системой).

{code:sql}
SELECT
  <измерения с полок «Строки», «Столбцы»>,
  <неагрегированные Меры с полок «Строки», «Столбцы»>, -- вычисляемые измерения
  <измерения с полки «Детализация»>,
  <выражения Мер с полки «Значения»>     AS <alias>,
  <выражения Мер с полки «Дополнительные»>  AS <alias>
FROM (
  /*
   * [Оригинальный SQL произвольного набора данных подставляется без изменений]
   */
) AS __base
WHERE
  <обычные WHERE-фильтры>
  [AND <выражения Мер с полки «Фильтры», тип WHERE>]
GROUP BY 
  <измерения с полок «Строки», «Столбцы»>,
  <неагрегированные Меры с полок «Строки», «Столбцы»>,
  <измерения с полки «Детализация»>
  -- GROUP BY присутствует только если в SELECT есть хотя бы одно агрегированное выражение
HAVING
  <выражения Мер с полки «Фильтры», тип HAVING, условие_1>
  [AND <выражение_N, условие_N>]
  -- для Сводной таблицы — алгоритм inject_having() (target_dimensions, CND, depth)
{code}

Допустима эквивалентная реализация через {{WITH __base AS (...)}}:
{code:sql}
WITH __base AS (
  /* [Оригинальный SQL произвольного набора данных] */
)
SELECT ... FROM __base WHERE ... GROUP BY ... HAVING ...
{code}

*Продуктовым требованием является не конкретная форма записи, а корректная инъекция выражений Мер в итоговые SQL-клаузы внешнего запроса.*

h4. Поведение Меры в зависимости от полки.

h5. Полка «Значения».
Выражение Меры подставляется в {{SELECT}}. {{GROUP BY}} формируется из полей «Строки», «Столбцы» и «Детализации». Если Мера неагрегированная, она попадает в {{SELECT}} без агрегирования (аналитик несёт ответственность за корректность); при этом {{GROUP BY}} расширяется этим выражением.

h5. Полка «Фильтры», тип {{WHERE}}.
Выражение Меры подставляется в {{WHERE}} — фильтрация происходит *до* {{GROUP BY}}, то есть отсекаются отдельные строки базового источника. Механизм {{target_dimensions}} / CND *не применяется*; блок «Применять на уровнях детализации» в UI при типе WHERE *скрыт*.

h5. Полка «Фильтры», тип {{HAVING}}.
Выражение Меры подставляется в {{HAVING}} — фильтрация происходит *после* {{GROUP BY}}, то есть отсекаются агрегированные группы.
* Для *«Сводной таблицы»* применяется алгоритм инъекции {{HAVING}} из *п. 2.2* постановки по HAVING-фильтрации ({{target_dimensions}}, CND, {{depth}}).
* Для *«Плоской таблицы»* применяется алгоритм из *п. 2.3* постановки по HAVING-фильтрации: при наличии агрегированных выражений условие инжектируется в {{HAVING}}; если агрегации нет — условие игнорируется.

h5. Полка «Строки»/«Столбцы».
Допускаются только *неагрегированные* Меры ({{aggregation_kind = 0}}). Выражение попадает одновременно в {{SELECT}} и {{GROUP BY}} — создавая «вычисляемое измерение».

h5. Полка «Детализация».
Мера попадает в {{SELECT}} и расширяет {{GROUP BY}} дополнительным уровнем группировки.

h5. Полка «Дополнительные».
Мера не попадает в запрос.

h5. Несколько Мер на одной полке.
Несколько Мер на «Значениях» — каждая становится отдельной колонкой в {{SELECT}}. Несколько Мер на «Фильтрах» объединяются по типу: все WHERE-условия — в {{WHERE}} через {{AND}}, все HAVING-условия — в {{HAVING}} через {{AND}}. Допускается одновременное наличие WHERE-Мер и HAVING-Мер на «Фильтрах» — они попадают в соответствующие клаузулы независимо.

----
h3. Устойчивость к изменениям метаданных.
* *Переименование Меры* разрывает все внешние ссылки на Меру (из визуализаций, настроек визуализаций, формул других Мер и Вычисляемых полей). Затронутые объекты получают статус {{invalid}} или ошибочное состояние. UI должен информировать аналитика о последствиях переименования (количество затронутых визуализаций и Мер) до подтверждения.
** Поле {{name}} - является ключом связи, {{id}} сохраняется как технический идентификатор для внутренних нужд системы, но не используется как ключ внешних ссылок.
* *Удаление Меры*: при подтверждении Мера получает флаг {{deleted = 1}} (мягкое удаление) и исчезает из всех пользовательских интерфейсов. Физически запись о Мере сохраняется в метаданных. Все ссылки на имя удалённой Меры в визуализациях и формулах других Мер становятся невалидными. Физическое удаление данных не выполняется, что позволяет автоматически «восстановить» связи при создании новой Меры с тем же именем.
** Если восстановить удалённую или создать новую Меру с прежним именем — ссылки восстановятся автоматически.
* *Изменение выражения Меры*: при пересохранении Меры все визуализации, использующие её, при следующем запросе данных автоматически получат обновлённое выражение (кэши запросов инвалидируются).
* *Изменение SQL набора данных*: при каждом сохранении SQL-запроса система перевалидирует все Меры набора (если применимо, см. «Жизненный цикл Меры после изменения SQL»).

----
h3. Обратная совместимость.
При отсутствии Мер на полках движок формирует запрос *тем же способом, что и в текущей системе*. Добавление поддержки Мер *не изменяет поведение* существующих визуализаций, если Меры в них не используются. Существующие вычисляемые поля виджетов не затрагиваются. Миграция существующих метаданных не требуется.

----
{panel:title=Интеграция с постановкой по HAVING-фильтрации.|borderStyle=dashed|borderColor=#0052cc|bgColor=#f0f4ff}
Поддержка мер в режиме фильтрации HAVING для обычных визуализаций (кроме Сводной таблицы) описывается в текущем эпике как *базовое поведение*.
* Следует обратить внимание, что, на данный момент, фильтрации HAVING в визуализациях (кроме Сводной таблицы) работает только с вычисляемыми полями (добавленными на полку "Фильтры") и не поддерживает его использование как глобального фильтра.

Поддержка уровня-специфичной HAVING-фильтрации для Сводной таблицы ({{apply_mode}}, {{target_dimensions}}, CND, {{depth}}) является *отдельным расширением* и описывается в связанной истории ([MB-4304|https://jira.modusbi.ru/browse/MB-4304]).

В текущем документе _История MB-4304_ фигурирует как *архитектурно связанный документ*, а не как обязательный этап разработки в рамках настоящей постановки.

Структура JSON-метаданных фильтра-меры для Сводной таблицы (HAVING с алгоритмом CND):

{code:json}
{ 
 "filters": [
  {
   "id": "6d3c3e58-8f23-45fd-9baf-90c123456789",
   "type": "measure",
   "measure_name": "Валовая прибыль",
   "filter_mode": "HAVING",
   "condition": "> 0",
   "apply_mode": "selected",
   "target_dimensions": ["col_month"]
  }
 ]
}
{code}

|| Поле || Тип || Описание ||
| {{type}}      | string  | {{"measure"}} — отличает от вычисляемых полей ({{"calculated"}}) |
| {{measure_name}}   | string  | Имя Меры; является ключом связи | 
| {{filter_mode}}   | string  | {{WHERE}} или {{HAVING}} |
| {{apply_mode}}   | string  | {{all}} или {{selected}}; только для {{HAVING}} (Сводная таблица) |
| {{target_dimensions}} | string[] | Массив ID измерений; только для {{HAVING}} Сводной таблицы; пустой при {{apply_mode: "all"}} |
| {{condition}}    | string  | Условие фильтрации: {{> 0}}, {{= 'Active'}} и т.д. | 
{panel}

---- 
h3. Приложенные материалы.
* Постановка «Использование Having при фильтрации данных в Сводной таблице»: [https://jira.modusbi.ru/browse/MB-4304]

---- 
h3. Критерии приёмки (AC).

*AC.1* — Доступность секции «Меры» и кнопки добавления.
* *Дано*: аналитик открыл настройки *произвольного* набора данных.
* *Тогда*: секция «Меры» отображается.
* *И*: кнопка «+ Добавить меру» активна только после того, как SQL набора данных задан, успешно проверен и из него получен актуальный состав полей.
* *И*: до этого кнопка {{disabled}} и показывает пояснение пользователю.

*AC.2* — Создание и редактирование Меры.
* При создании/редактировании Меры через модальное окно доступны поля: {{name}}, {{description}}, {{data_type}}, {{aggregation_kind}}, {{hierarchy}}, {{level}}, {{expression}}.
* При сохранении система создаёт/обновляет Меру в метаданных набора.
* Выполняются проверки обязательных полей, уникальности названия, валидности выражения и корректности ссылок.

*AC.3* — Табличный список Мер.
* Сохранённые Меры отображаются в табличном списке.
* Для каждой Меры видны: название, тип данных, агрегированность, иерархия, уровень, краткое выражение, действия.
* Для агрегированных Мер отображается ярлык {{AGR}}.

*AC.4* — Видимость Мер в конструкторе и связанных селекторах.
* При построении визуализации на основе набора данных с Мерами в field picker отображается секция «Меры».
* Меры имеют отдельную визуальную иконографию и tooltip.
* Меры доступны не только для drag-and-drop на полки, но и во всех совместимых селекторах полей текущей визуализации; неподдержанные места явно обозначены как ограничение этапа. 

*AC.5* — Статус {{invalid}}.
* *Дано*: Активная Мера перестала быть валидной (например, после изменения SQL набора данных или переименования/удаления другой связанной Меры).
* *Тогда*: такая Мера не удаляется автоматически, получает статус {{invalid}}, подсвечивается в списке Мер в визуализациях, показывая причину ошибки, а также становится недоступна для нового добавления на полки.
* *И*: если визуализация уже содержит ссылку на такую Меру, пилюля отображается в error-состоянии, а запрос данных блокируется с понятным сообщением о причине ошибки.

*AC.6* — Техническая форма базового слоя не является критерием приёмки.
* При размещении Меры на поддержанной полке её выражение корректно инжектируется во внешний запрос построителя в зависимости от типа полки: {{SELECT}}, {{WHERE}}, {{GROUP BY}}, {{HAVING}}.
* Конкретная форма базового слоя ({{FROM (<sql>) AS __base}} или {{WITH __base AS (...)}}) не является критерием приёмки.

*AC.7* — Полка «Значения».
* *Дано*: Мера {{SUM("revenue") - SUM("cost")}} размещена на полке «Значения», а поле {{month}} — на полке «Строки».
* *Тогда*: итоговый внешний SQL содержит выражение Меры в {{SELECT}}, а группировка соответствует текущим правилам query builder для данной визуализации.
* *И*: для неагрегированной Меры на полке «Значения» пользователь получает предупреждение о возможном изменении уровня детализации.

*AC.8* — Полка «Фильтры», тип WHERE.
* *Дано*: Мера с {{aggregation_kind = 0}} и выражением {{ "operation_type" <> 'Возврат' }} размещена на полке «Фильтры» с типом {{WHERE}}.
* *Тогда*: итоговый внешний SQL содержит это выражение в {{WHERE}}.
* *И*: выбор {{WHERE}} для Меры с {{aggregation_kind = 1}} блокируется.

*AC.9* — Полка «Фильтры», тип HAVING.
* *Дано*: Мера с {{aggregation_kind = 1}} и выражением {{ SUM("revenue") - SUM("cost") }} размещена на полке «Фильтры» с типом {{HAVING}}, условие {{> 0}}, визуализация не требует специальной pivot-логики.
* *Тогда*: итоговый внешний SQL содержит условие в {{HAVING}}.
* *И*: выбор {{HAVING}} для Меры с {{aggregation_kind = 0}} блокируется.

*AC.10* — Полки «Строки», «Столбцы», «Детализация».
* *Дано*: Мера с {{aggregation_kind = 1}} перетаскивается на полку «Строки», «Столбцы» или «Детализация».
* *Тогда*: размещение блокируется, а пользователь получает соответствующее сообщение.
* *И*: Мера с {{aggregation_kind = 0}}, размещённая на этих полках, попадает в {{SELECT}} и {{GROUP BY}} внешнего запроса.

*AC.11* — Переименование Меры.
* *Дано*: Мера используется в одной или нескольких визуализациях и/или в выражении другой Меры.
* *Когда*: пользователь переименовывает Меру.
* *Тогда*: система показывает предупреждение с числом затронутых визуализаций и Мер.
* *И*: после подтверждения все ссылки на прежнее имя становятся невалидными; затронутые пилюли переходят в error-состояние; зависимые Меры получают статус {{invalid}}.
* *И*: при создании новой Меры с прежним именем все ссылки автоматически восстанавливаются.

*AC.12* — Удаление Меры.
* *Дано*: Мера A используется в формуле Меры B; Мера B размещена на полке Визуализации_1.
* *Когда*: Мера A удаляется (получает {{deleted = 1}}).
* *Тогда*: Мера A исчезает из списка Мер в настройках набора данных и из боковой панели конструктора.
* *И*: Мера B получает статус {{invalid}} с читаемой причиной ошибки.
* *И*: Визуализация_1, содержащая Меру B на полке, переходит в error-состояние с блокировкой запроса данных.
* *И*: при создании новой Меры с именем Меры A система ревалидирует Меру B; если выражение Меры B корректно - она получает статус {{valid}}, а Визуализация_1 восстанавливает работоспособность.

*AC.13* — Обратная совместимость и ограничение этапа 1.
* При построении визуализации без использования Мер построитель запросов сохраняет текущее поведение системы.
* Добавление поддержки Мер не изменяет результаты существующих визуализаций, если Меры в них не используются.
* Для простых и составных наборов данных секция «Меры» отсутствует, а создание мер недоступно.
