В рамках доработки нашего аналитического портала Modus BI, мной уже был подготовлен ряд постановок.
Формв ввода данных (ФВД) - это отдельное приложение имеет достаточно сильную изоляцию от основной части нашего аналитического портала (особенно по части фронтенда), ФВД имеет свои зависимости. Однако, бэкенд при работе видит их как общую версию фронтенда, т.е. Frontend-части системы обладают единым маркером версии для бэкенда:
** *Основной портал* — доступен по корневому URL ({{/}}), имеет собственное окно аутентификации и авторизации.
** *ФВД (Форма ввода данных)* — доступен по отдельному URL ({{/fvd}}), также обладает собственным окном аутентификации/авторизации.
Но при прохождении аутентификации одним из способов мы получаем единый JWT-токен, который даёт доступ к любым частям платформы (в сообщетствии с правами пользователя).
Ранее мною уже была подготовлена постановка по внедрению "безлимитного поиска в выпадающих списках в ФВД и данная постановка уже была реализована - "h3. Контекст и проблематика:

На данный момент в Формах ввода данных (ФВД) выпадающие списки имеют ограничение на вывод и поиск элементов (лимит в 5000 записей, по факту для оптимизации рендеринга может подгружаться около 500). Поиск осуществляется только силами фронтенда по уже загруженному пулу данных. В условиях больших справочников, это делает невозможным поиск и выбор нужных записей, не попавших в первоначальную выборку.
----
h3. Цель задачи:

Аналогичная проблема раньше существовала в фильтрах основного портала. Она решена через опцию «Режим поиска без лимита» в настройках отображения фильтра (контейнер фильтров → настройки отображения).

*Требуется:*
Реализовать в ФВД для компонента «Выпадающий список» функционал *«Поиск без лимита»* (серверный поиск), аналогичный тому, который уже успешно реализован и применяется в контейнере Фильтров основного портала.

*User Story:* > Как аналитик, я хочу иметь возможность включать серверный поиск для выпадающих списков в ФВД, чтобы пользователи могли искать значения по всему справочнику, не ограничиваясь первыми 5000 записей.
----
h3. Описание реализации:

В настройки отображения компонента «Выпадающий список» (в конструкторе ФВД) необходимо добавить новые опции (по аналогии с фильтрами):
 # {*}Переключатель (Toggle switch) «Режим поиска без лимита» ({_}по умолчанию выключен{_}){*}.
 ** *Описание:* Снимает фронтовые ограничения на количество получаемых уникальных значений. Переводит работу компонента с локального поиска на серверный (отправка запросов на бэкенд при вводе текста).
 ** *При активации этого переключателя должны отображаться две дополнительные настройки - {_}пункты 2 и 3{_}:*
 # {*}Поле «Количество символов для активации режима» (числовое){*}.
 ** *Описание:* Задает минимальное количество введенных символов, после которых инициируется запрос к БД (глобальный поиск).
 ** *Поведение:* Пока введено меньше символов, чем указано в настройке, поиск не триггерится.
 # {*}Переключатель «Не загружать значения»{*}.
 ** *Описание:* Если опция включена, при открытии формы выпадающий список изначально пуст (значения не подгружаются).
Список ожидает ввода пользователя и начинает заполняться только результатами поиска после ввода необходимого количества символов (п. 2).

----
h3. Технические требования и ограничения

Задача требует аккуратной адаптации существующего кода из основного портала. Компонент в ФВД имеет легаси-особенности.
 # *Компонент UI:* Используется {{{}ReSelect{}}}. Необходимо перенести *только логику* из доработанного компонента фильтров (автор правок: {_}Валерий Маланин{_}), а не заменять компонент целиком.
 # *Проблема потери фокуса:* При вводе символов компонент *не должен терять фокус* (курсор не должен сбрасываться).
 ** Не использовать динамический {{key}} для компонента, чтобы избежать лишних ререндеров всего узла. (Учесть правки, которые ранее делались для портала).
 # *Обратная совместимость:* Сохранить текущую стилизацию компонента в ФВД (старый стиль).
 ** Сохранить работу *всплывающих подсказок* («Подсказка по заполнению поля»), которые настраиваются в ФВД. Текст подсказки не должен сломаться при внедрении новой логики поиска.
 # *Зависимости:* Учесть, что ФВД использует старые версии пакетов. Возможны конфликты, требуется проверка совместимости.
 # *Запросы на backend:* Серверный поиск должен {_}дебаунситься{_}, чтобы не спамить бэкенд запросами при каждом нажатии клавиши до достижения нужного лимита символов или при быстром вводе.

h4. Подготовленный датасет на +6000 записей+ и ФВД - [https://dev.modusbi.ru/fvd/form/47]
 * из _6000_ записей в выпадающем списке видны {_}4991{_}.

h4. Запись встречи - [https://modusbi.ktalk.ru/recordings/5DHLYOMkVRluiO2zWd4V]
----
h3. Критерии приёмки (AC)
 * AC1. *Настройки:* В режиме редактирования ФВД для выпадающего списка доступны новые настройки: тумблер «Режим поиска без лимита», поле «Колличество символов для активации режима» и тумблер «Не загружать значения» - последние два доступны только при активации тумблера «Режим поиска без лимита».
 * AC2. *Локальный поиск (по умолчанию):* Если настройка отключена, компонент работает по старому сценарию (локальный поиск {*}по загруженному лимиту{*}).
 * AC3. *Отложенный поиск:* Если задано, «Количество символов = {_}n{_}», запросы на бэкенд не уходят если введено менее _n_ символов.
Например, при установке «Количество символов = 3», запросы на бэкенд не уходят при вводе 1 или 2 символов. Запрос отправляется только после ввода 3-го символа.
 * AC4. *Ленивая загрузка:* Если включена опция «Не загружать значения», при клике на пустой селект - список пуст. При вводе нужного количества символов появляются релевантные данные с бэкенда.
 * AC5. *UX ввода:* При вводе текста в строку поиска курсор остаётся на месте, фокус с поля не сбрасывается (нет перерендера компонента из-за изменения {{{}state/key{}}}).
 * AC6. *Сохранность легаси:* Всплывающие подсказки к полю и прочие дополнительные элементы отображаются корректно. Визуальный стиль компонента не изменился.
 * AC7. *Производительность:* Компонент протестирован на справочнике > 5 000 элементов — поиск работает стабильно, без тормозов и утечек памяти.
 * AC8. *Функциональность:* Доработка успешно собирается и работает в специальной сборке для РСХН (3.12 + обновлённый ФВД).".

Далее мной была подготовлена ещё одна постановка по доработки функциональности безлимитного (серверного) поиска на ФВД с учётом дополнительных выбранных полей - "h3. Контекст и проблематика:

Ранее в Формах ввода данных (ФВД) был реализован функционал «Поиск без лимита» (серверный поиск) для компонента «Выпадающий список». Однако, в ходе эксплуатации и тестирования была выявлена проблема с фильтрацией при наличии дополнительных настроек компонента.

В конструкторе ФВД для выпадающего списка можно настроить *«Информационные поля»* — дополнительные колонки справочника, которые выводятся пользователю для большей информативности. Сейчас при серверном поиске фронтенд отправляет запрос, передавая в массиве {{where}} только *одно* поле (основное, без вспомогательных). 
В результате поиск осуществляется только по основному наименованию, игнорируя значения из выбранных информационных полей.

Следовательно, текущий серверный поиск в ФВД реализован неполно и не воспроизводит ожидаемую логику компонента.
----
h3. 1. Цель задачи:

*Требуется:*
Доработать серверный поиск компонента «Выпадающий список» в ФВД так, чтобы при включенном режиме *«Поиск без лимита»* поиск выполнялся {*}по всем полям, которые должны участвовать в поиске по конфигурации компонента{*}, а объединение условий происходило на бэкенде по логике *ИЛИ (OR)* в рамках *одного* запроса.

*Правило участия полей в поиске:*
 # *Основное поле* выпадающего списка участвует в поиске {*}всегда{*}.
 # *Информационные поля* участвуют в серверном поиске {*}только если{*}:
 ** они выбраны в секции {*}«Информационные поля»{*};
 ** для компонента включена опция {*}«Показ внутри меню»{*}.
 # Если {*}«Показ внутри меню» выключен{*}, серверный поиск выполняется {*}только по основному полю{*}.
 # Данная доработка {*}не должна менять существующую логику отображения и выводимых подсказок{*}; задача касается только корректного формирования и обработки серверного поиска.

*User Story:*
Как пользователь системы, я хочу находить нужную запись в выпадающем списке ФВД не только по основному наименованию, но и по дополнительным реквизитам (информационным полям), если они настроены для участия в поиске, чтобы быстро выбирать запись по любому известному мне атрибуту.
----
h3. 2. Описание целевого поведения:

Если для компонента «Выпадающий список» включён {*}«Поиск без лимита»{*}, то при вводе пользователем поисковой строки система должна выполнять *один* серверный поиск со следующей логикой:
 # Если дополнительные _информационные поля_ {*}не настроены{*}:
 ** поиск выполняется только по основному полю.
 # Если информационные поля {*}настроены{*}, но {*}«Показ внутри меню» выключен{*}:
 ** поиск выполняется только по основному полю;
 ** визуальное отображение дополнительных полей и текущее поведение подсказок сохраняются.
 # Если информационные поля *настроены* и {*}«Показ внутри меню» включен{*}:
 ** поиск выполняется по основному полю и по всем выбранным информационным полям;
 ** объединение условий выполняется по логике {*}OR (ИЛИ){*};
 ** запись должна попасть в результат, если поисковая строка найдена *хотя бы в одном* из участвующих полей.

*Пример целевой логики:*
Есть выпадающий список с основным полем {{name}} и выбранными информационными полями {{ent_name}} и {{{}inn{}}}.
При включенном *«Показ внутри меню»* и вводе строки {{ЛПХ Шишко}} backend должен отобрать записи по логике, эквивалентной (ориентировочно):
{code:sql}
name LIKE '%ЛПХ Шишко%' OR ent_name LIKE '%ЛПХ Шишко%' OR inn LIKE '%ЛПХ Шишко%'
{code}
*Важно:* пример приведён как {*}логическая модель поведения{*}, а не точный формат.
----
h3. 3. Требования к реализации:
h4. 3.1. Backend-часть:
 * Доработать backend-логику, обслуживающую серверный поиск для ФВД (текущий сценарий на базе команды {{data.list}} или соответствующего абстрактного обработчика получения данных).
 * Backend должен уметь принимать *набор поисковых полей* и *одну поисковую строку* и строить по ним единое условие поиска с логическим оператором {*}OR{*}.
 * Поиск по нескольким полям должен выполняться {*}в рамках одного HTTP-запроса{*}.
 * Backend-реализация должна быть {*}обратно-совместимой{*}: текущие сценарии, где передаётся только одно поле поиска, должны продолжать работать без изменений.
 * Backend-реализация должна быть достаточно {*}абстрактной{*}, а не «захардкоженной» только под один конкретный кейс ФВД, +чтобы механизм OR-поиска+ по массиву полей {+}можно было переиспользовать в других частях платформы{+}.
 * При совпадении по нескольким полям одна и та же запись должна возвращаться {*}один раз{*}.
 * Текущая сортировка / порядок выдачи, задаваемые настройками списка, не должны ломаться из-за появления много-польного поиска.
 * Решение не должно быть привязано к одному конкретному SQL-диалекту; необходимо учесть поддерживаемые backend-хранилища ФВД.
 * Если в текущем backend уже существует механизм объединения условий по OR, необходимо {*}переиспользовать его{*}, а не вводить отдельную частную реализацию только для ФВД.

h4. 3.2. Frontend-часть ФВД:
 * Доработать формирование запроса для режима {*}«Поиск без лимита»{*}.
 * При подготовке payload необходимо формировать *массив поисковых полей* по следующему правилу:
 ** всегда включать в поиск *основное поле* компонента;
 ** дополнительно включать *все выбранные информационные поля* только при включенной настройке {*}«Показ внутри меню»{*}.
 * Перед отправкой запроса необходимо выполнять дедупликацию списка полей, чтобы одно и то же поле не попало в payload несколько раз.
 * Поиск должен оставаться {*}debounce{*}-зависимым, чтобы не инициировать лишние запросы при быстром вводе.
 * Логика настройки *«Количество символов для активации режима»* должна сохраниться без изменений.
 * Логика настройки *«Не загружать значения»* должна сохраниться без изменений.
 * При обновлении списка по результатам серверного поиска {*}компонент не должен терять фокус{*}; курсор в поисковой строке должен оставаться на месте.

h4. 3.3. Технические требования и ограничения:
 # *Единый запрос:* Поиск по нескольким полям должен выполняться {*}строго в одном HTTP-запросе{*}.
 # *Обратная совместимость:* Изменение backend-контракта не должно ломать основной портал и существующие сценарии ФВД, использующие текущую версию обработчика.
 # *Должна сохраниться функиональность уже реализованных механизмов:*
 ** переключатель {*}«Поиск без лимита»{*};
 ** порог {*}«Количество символов для активации режима»{*};
 ** переключатель {*}«Не загружать значения»{*};
 ** дебаунс запросов и т.д.
 # *Общие требования:*
 ** Сохранить текущую общую стилизацию и логику выпадающего списка и подсказок в ФВД.
 ** После внедрения доработки, всплывающие подсказки и выпадающий список должны работать не хуже текущего поведения.
 ** Сценарии с большими справочниками должны отрабатываться без визуальных зависаний и без утечек памяти.

h4. 3.4. Материалы / воспроизведение:
 * Подготовленный датасет на {+}2000 записей{+} ФВД: [https://dev.modusbi.ru/fvd/form/51]
 * Запись обсуждения: [^Запись обсуждения_ФВД_Информационное_поле_Поиск_без_лимита.mp4]

----
h3. 4. Критерии приёмки (AC):
 * AC1. *Без новых настроек:* В UI конструктора ФВД не появляются новые настройки. Используются существующие настройки компонента: «Поиск без лимита», «Количество символов для активации режима», «Не загружать значения», «Информационные поля», «Показ внутри меню» и т.д.
 * AC2. *Обратная совместимость локального режима:* Если «Поиск без лимита» выключен, компонент работает по прежнему локальному сценарию без изменения текущего поведения.
 * AC3. *Правило участия полей:* При включенном серверном поиске основное поле всегда участвует в поиске. Информационные поля участвуют только если они выбраны в настройках компонента и при этом включен переключатель «Показ внутри меню».
 * AC4. *OR-поиск:* Если в поиске участвует несколько полей, бэкенд применяет к ним логическое условие {*}ИЛИ (OR){*}. Запись возвращается, если поисковая строка найдена хотя бы в одном из полей.
 * AC5. *Один запрос:* Поиск по нескольким полям выполняется в рамках *одного* HTTP-запроса. Решение через несколько запросов с фронтенда и последующее объединение результатов на клиенте отсутствует.
 * AC6. *Без дублей:* Если одна запись совпала сразу по нескольким полям, в выдаче она присутствует только один раз.
 * AC7. *Отложенный запуск:* Настройка «Количество символов для активации режима» продолжает работать. Пока введено меньше заданного количества символов, запрос на backend не отправляется.
 * AC8. *Ленивая загрузка:* Если включена опция «Не загружать значения», при открытии формы список пуст. После ввода нужного количества символов появляются только релевантные результаты серверного поиска.
 * AC9. *Сохранение UX ввода:* При вводе текста в строку поиска фокус не теряется, курсор не сбрасывается из-за обновления списка.
 * AC10. *Отсутствие UI изменений:* Визуальный стиль компонента, отображение дополнительных текстов, подсказок и т.д. не ломается и не изменяется визуально.
 * AC11. *Обратная совместимость конфигураций:* Выпадающие списки без настроенных информационных полей продолжают работать штатно и не требуют каких-либо изменений в настройке.
 * AC12. *Производительность:* Компонент протестирован на справочнике > 5 000 элементов; поиск работает стабильно, без заметных просадок производительности, каскада лишних запросов и ошибок в логах.
 * AC13. *Сортировка:* После внедрения много-польного поиска не нарушается текущий порядок сортировки элементов, заданный существующими настройками списка.
 * AC14. *Backend-совместимость:* Изменение backend-логики не ломает существующие сценарии выборки данных, где используется одинарное условие поиска по одному полю.".

Обе написанные постановки уже реализованы в рамках доработки нашего портала, а конкретно его ФВД-части.

На данный момент доработки требуются в рамках основного аналитического портала, а не в рамках Форм ввода данных (ФВД). В основном портале в компоненте иерархические фильтры не работает настройка «Поиск без лимита». При активации этой настройки и последующем вводе поискового запроса - механизм серверного поиска не срабатывает, а поиск продолжает выполняться на фронтенде со всеми вытекающими ограничениями, т.е. подгружается только ограниченное колличество записей.

В данной компоненте нельзя выбрать дополнительные «Информационные поля», но сам компонент иерархического фильтра позволяет задать иерархию из нескольких полей и использовать их все для фильтрации данных в визуализациях. В выпадающем списке данный компонент отображает поля в заданной иерархии и позволяет выбирать одно или несколько полей в иерархическом режиме. Поиск в этом выпадающем списке таже происходит по всем полям в иерархии.

На самом деле, по сути он задействует несколько независимых фильтров по определённой иерархической логике, но того же результата можно добится при помощи нескольких независимых элементов "Контейнер с фильтрами", который позволяет выбрать только одно поле и использовать его для фильтрации данных в визуализациях. При этом важно заметить, что если добавить и компонент "иерархические фильтры" и выбрать там три поля (например, "Категория", "Подкатегория" и "Город") и, в рамках этого же дашборда создать ещё три независимых элемента "контейнер с фильтрами" и для каждого независимо выбрать по одному полю из полей "Категория", "Подкатегория" и "Город", то в момент когда будет задаваться иерарфический фильтр (выбирающий сразу все три поля), в несвязанных "контейнерах с фильтрами" (с этими же полями) автоматически проставляются значения, которые были выбраны в "иерархическом фильтре". То есть по сути - это одни и теже фильтры, доступные из разных мест. 

Вот пример запроса отправляемого при открытии выпадающего списка такого иерархического фильтра (с выбранными понями:
1)Город,
2)Дата,
3)Категория,
4)Подкатегория)
-> "
curl 'https://dev.modusbi.ru/v1/api/recordsets/443' \
  -H 'accept: */*' \
  -H 'accept-language: ru-RU' \
  -H 'authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiIsImtpZCI6OTgwNn0.eyJpZCI6NjcsInNlc3Npb25faWQiOiI2NGZiYWJlMS05ZmZkLTQ1ZjYtYmE0ZC1hODIzYTdmNDY1MGQiLCJ1c2VybmFtZSI6InYubWVkaW50c2V2Iiwicm9sZSI6ImFkbSIsImlhdCI6MTc3OTEyMDI2MiwiZXhwIjoxNzc5MjA2NjYyLCJsYW5ndWFnZSI6InJ1LVJVIn0.ctovhONqKXafxyyuVJ-spbE4xeSqb8gJNSK8XqN8FPE' \
  -H 'cache-control: no-cache' \
  -H 'content-type: application/json; charset=UTF-8' \
  -b 'GUEST_LANGUAGE_ID=ru_RU; COMPANY_ID=20102; ID=47594762526f716e443852347568576e6f65787347413d3d' \
  -H 'dnt: 1' \
  -H 'origin: https://dev.modusbi.ru' \
  -H 'pragma: no-cache' \
  -H 'priority: u=1, i' \
  -H 'referer: https://dev.modusbi.ru/report/783' \
  -H 'sec-ch-ua: "Chromium";v="146", "Not-A.Brand";v="24", "Google Chrome";v="146"' \
  -H 'sec-ch-ua-mobile: ?0' \
  -H 'sec-ch-ua-platform: "Linux"' \
  -H 'sec-fetch-dest: empty' \
  -H 'sec-fetch-mode: cors' \
  -H 'sec-fetch-site: same-origin' \
  -H 'user-agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36' \
  --data-raw '{"group":[],"order":[{"name":"Gorod","alias":"[values][0]","calc":""},{"name":"Data","alias":"[values][1]","calc":""},{"name":"Kategorija","alias":"[values][2]","calc":""},{"name":"Podkategorija","alias":"[values][3]","calc":""}],"distinct":1,"select":[{"name":"Gorod","aggr":"","alias":"[values][0]","calc":""},{"name":"Data","aggr":"","alias":"[values][1]","calc":""},{"name":"Kategorija","aggr":"","alias":"[values][2]","calc":""},{"name":"Podkategorija","aggr":"","alias":"[values][3]","calc":""}],"where":[],"having":{"condition":""},"series":{"enable":0},"limit":0}'
".


На основной части портала поиск в выпадающем списке по введенному наименованию проходит на фронте и работает корректно - он находит введённое наименование на любом уровне иерархии и позволяет выбрать его в иерархическом фильтре (также активируются фильтры по всех вышестоящим участникам иерархии). Т.е. Стандартный поиск (без включения параметра «Поиск без лимита») работает корректно. Однако при активации режима «Поиск без лимита» (серверный поиск) продолжает использоваться аналогичный режим поиска на фронтовой части.

Требуется:
1) Реализовать логику работы «Поиска без лимита» в иерархических фильтрах.
2) Обеспечить корректную отправку запроса на сервер с учётом всех уровней иерархии при включенной опции «Поиск без лимита».
3) Исключить блокировку запроса клиентским кэшем. Обеспечить корректное функционирование настройки «Количество символов для активации режима» - после ввода достаточного колличества символов (выбранного в настройках) должен отправляться корректный запрос на бэкенд для проведения серверного поиска без лимита и ограничений по колличеству выводимых строк.
4) Также настройка "Не загружать значения" должна работать корректно в этом компоненте.

Также прикладываю во вложении транскрипцию обсуждения данной задачи с коллегой.

Как системный аналитик с большим стажем, составь постановку задачи для реализации необходимых доработок разработчиками.
Постановка должна быть понятной разработчикам, достаточной, однозначной, талантливо-написанной и включать все необходимые разделы, включая критерии приёмки (AC). При написании данной постановки сохрани и используй разметку из предыдущей постановки, совместимую для публикации в JIRA.

